import 'package:flutter/material.dart';

class block extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      height: 40,
      width: 40,
      child: Image.asset('lib/images/block.png'),
    );
  }
}
