import 'package:flutter/material.dart';

class pole extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      height: 200,
      width: 200,
      child: Image.asset('lib/images/pole.png'),
    );
  }
}
